<Container>
  <LinkTo @route="artists" class="nav-artists block md:hidden text-sm font-semibold pb-2">/artists</LinkTo>
  {{outlet}}
  <Grid @artists={{true}}>
    {{#each @model as |artist|}}
      <ArtistTile @artist={{artist}} />
    {{/each}}
  </Grid>
</Container>
