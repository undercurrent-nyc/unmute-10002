<Container>
  <LinkTo @route="teams" class="nav-teams block md:hidden text-sm font-semibold pb-2">/teams</LinkTo>
  <Grid>
    {{#each @model as |team|}}
      <TeamTile @team={{team}} />
    {{/each}}
  </Grid>
</Container>
